# 安全政策

简体中文 · [English](SECURITY.en.md)

## 当前阶段

澄音 Companion 仍处于本地原型阶段，尚未发布受支持的公开版本。创建正式 GitHub 仓库后，将启用 GitHub Private Vulnerability Reporting。

## 报告范围

请私下报告以下问题，不要先开公开 Issue：

- 内容包路径穿越、签名绕过或任意代码执行；
- 许可证权益伪造或购买数据泄露；
- 更新器劫持或回滚保护失效；
- Codex 事件桥接读取了预期范围外的数据；
- 用户设置、备份或诊断数据泄露；
- 安装包签名或公证异常。

普通崩溃、界面问题和无安全影响的内容包兼容问题可以使用公开 Issue。

## 秘密与凭证

- 仓库和 App Bundle 不保存火山引擎、支付、签名或 Apple Developer 私钥。
- 本地开发使用环境变量或系统钥匙串。
- 日志必须移除令牌、购买记录和可识别的本地路径。
- 一旦凭证误提交，应立即吊销；仅删除 Git 历史不足以恢复安全。

提交或打包前运行 `python3 scripts/audit-public-source-secrets.py --json`。
它对公开源码白名单执行离线、有界扫描，拒绝私钥、常见提供商令牌、内嵌基础认证、
疑似凭证赋值和高风险凭证文件；回执不含匹配内容，不读取环境变量值或用户私有目录。
这是意外泄漏防线，不是恶意代码审计，也不能证明 Git 历史从未包含秘密。

## 内容安全

公开版本只接受有明确使用权的成年虚构角色素材。涉及真人肖像、声音、音乐或视频权利的争议，应立即暂停分发相关内容包，保留安全撤回和退款能力。
